#include <stdio.h>
/**
 * main - entry point
 * Return: 0
 */
int main(void)
{
	char x;

	for (x = '0'; x <= 9; x++)
		putchar(x);
	for (y = 'f'1; y <= 'f'; x++)
		putchar(y);
	putchar('\n');

	return (0);
}
